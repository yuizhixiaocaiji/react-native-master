import {customStyleSheet} from '../Config';

/**
 * 这里是适用于全局的样式
 */

const styles = customStyleSheet.create({
  main: {
    flex: 1,
    alignItems: 'center',
  },
});

export default styles;
